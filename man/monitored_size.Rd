% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitored_oc.R
\name{monitored_size}
\alias{monitored_size}
\title{Attained size of the monitored procedure over a null grid}
\usage{
monitored_size(
  n,
  m,
  tau,
  null_rates,
  alpha = 0.05,
  driver = c("exact", "asymptotic"),
  region = NULL,
  cp_grid = NULL
)
}
\arguments{
\item{n}{Integer. Subjects per arm at the final analysis.}

\item{m}{Integer. Completely observed subjects per arm at the
interim analysis, \verb{0 < m < n}.}

\item{tau}{Numeric. Futility threshold: stop when conditional
power is below \code{tau}.}

\item{null_rates}{Numeric vector of common event rates under
the null hypothesis.}

\item{alpha}{Numeric. One-sided significance level.}

\item{driver}{Character. \code{'exact'} or \code{'asymptotic'}.}

\item{region}{Optional precomputed rejection region.}

\item{cp_grid}{Optional precomputed matrix from
\code{\link[=conditional_power_grid]{conditional_power_grid()}}; supplying it avoids recomputation
across thresholds and true-rate settings.}
}
\value{
Numeric vector of attained rejection probabilities,
one per element of \code{null_rates}.
}
\description{
Evaluates \code{\link[=monitored_operating_characteristics]{monitored_operating_characteristics()}} under
\code{p_treatment = p_control = p} for each \code{p} in \code{null_rates} and
returns the rejection probabilities; their maximum estimates
the attained size of the monitored procedure.
}
