% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_conditional_power.R
\name{count_conditional_power_nb}
\alias{count_conditional_power_nb}
\title{Conditional power under gamma-Poisson (negative-binomial)
heterogeneity, applying the conditional Poisson test's
(nominal) rejection region}
\usage{
count_conditional_power_nb(
  subjects1,
  subjects2,
  theta1,
  theta2,
  phi,
  alpha = 0.025,
  p0 = NULL,
  tol = 1e-10
)
}
\arguments{
\item{subjects1, subjects2}{Data frames (or \code{NULL} for a
homogeneous-exposure shortcut) with columns \code{n} (events so
far), \code{c} (exposure so far), and \code{c_future} (remaining exposure)
for each at-risk subject in the arm.}

\item{theta1, theta2}{Assumed future event rates per unit exposure
in each arm.}

\item{phi}{Numeric \eqn{\ge 0}. Common frailty dispersion assumed
for both arms.}

\item{alpha}{One-sided level.}

\item{p0}{Null exposure-weighted share used to define the
rejection region, \code{p0 = future_exposure1 / (future_exposure1 + future_exposure2)} under \eqn{H_0} of
common rate and no further complication; supplied explicitly to
allow a design-stage exposure ratio distinct from the observed
interim ratio.}

\item{tol}{Truncation tolerance passed to \code{\link[=nb_predictive_pmf]{nb_predictive_pmf()}}
for each subject; per-subject truncation errors are additive to
a first order and the achieved total is returned.}
}
\value{
A list with \code{power}, \code{p0}, and \code{trunc_error} (sum of the
per-subject truncation errors, an upper bound on the resulting
error in \code{power}).
}
\description{
Extends \code{\link[=count_conditional_power_poisson]{count_conditional_power_poisson()}} to \code{phi > 0} by
convolving the per-subject negative-binomial predictive pmfs
(\code{\link[=nb_predictive_pmf]{nb_predictive_pmf()}}) into an arm-level future-count
distribution, then applying the same conditional-Poisson
rejection rule used in the pure-Poisson case. This is exact
\emph{conditional on the assumed frailty model for the future data},
but note carefully: when \code{phi > 0} the conditional Poisson test's
classical exact-size guarantee (which relies on the arm totals
being Poisson, not negative binomial) no longer holds, so the
quantity returned is the exact probability that the \emph{nominal}
conditional-Poisson decision rule rejects when the true generating
process is negative binomial, not the exact power of a test with
guaranteed exact size. This function does not compute or claim an
exact-size test for \code{phi > 0}; see the manuscript's discussion of
this distinction.
}
