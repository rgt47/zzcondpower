% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_conditional_power.R
\name{count_conditional_power_poisson}
\alias{count_conditional_power_poisson}
\title{Exact conditional power of the conditional Poisson (Przyborowski-
Wilenski) final test, pure-Poisson case}
\usage{
count_conditional_power_poisson(
  x1,
  x2,
  future_exposure1,
  future_exposure2,
  theta1,
  theta2,
  alpha = 0.025,
  p0 = NULL
)
}
\arguments{
\item{x1, x2}{Interim event counts in arm 1 (treatment) and arm 2
(control).}

\item{future_exposure1, future_exposure2}{Total remaining exposure
(sum across at-risk subjects) to the final analysis in each
arm.}

\item{theta1, theta2}{Assumed future event rates per unit exposure
in each arm.}

\item{alpha}{One-sided level.}

\item{p0}{Null exposure-weighted share used to define the
rejection region, \code{p0 = future_exposure1 / (future_exposure1 + future_exposure2)} under \eqn{H_0} of
common rate and no further complication; supplied explicitly to
allow a design-stage exposure ratio distinct from the observed
interim ratio.}
}
\value{
A list with \code{power}, and \code{rejection_region} (a logical
vector over \verb{k = 0,...,Dmax} future total events, \code{TRUE}
where the arm-1 split rejects at that total).
}
\description{
In the absence of frailty (\eqn{\phi = 0}), each subject's future
count is Poisson, so each arm's future total is exactly Poisson
(sum of independent Poissons), and the conditional Poisson test
given the total final event count \eqn{D} has an exact fixed
rejection region (Przyborowski and Wilenski, 1940). This function
enumerates that rejection region exactly and sums its probability
under the specified rates, conditional on the interim event
counts.
}
