% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/halperin.R
\name{halperin_conditional_power}
\alias{halperin_conditional_power}
\title{Asymptotic conditional power in the Halperin tradition}
\usage{
halperin_conditional_power(
  n,
  m,
  p_treatment,
  p_control,
  theta = NULL,
  alpha = 0.05
)
}
\arguments{
\item{n}{Integer. Subjects per arm at the final analysis.}

\item{m}{Numeric. Effective completely observed subjects per arm
at the interim analysis; must be less than \code{n}.}

\item{p_treatment}{Numeric. Interim event-rate estimate in the
treatment arm.}

\item{p_control}{Numeric. Interim event-rate estimate in the
control arm.}

\item{theta}{Numeric. Assumed future treatment effect on the
difference scale, \code{p_control - p_treatment}; defaults to the
observed interim difference (current-trend conditional power).}

\item{alpha}{Numeric. One-sided significance level of the final
test.}
}
\value{
The asymptotic conditional power, a single numeric
value.
}
\description{
Computes the normal-approximation conditional power for the
two-proportion comparison, in the z-statistic formulation that
descends from Halperin et al. (1982) and that standard
conditional-power software implements (Jennison and Turnbull
2000, Section 10.2; the same formulas appear in the PASS
documentation). The interim z-statistic is carried forward under
an assumed future treatment effect on the difference scale, with
statistical information \eqn{I = m / (2 \sigma^2)} for \code{m}
effective subjects per arm and \eqn{\sigma^2 = \bar p (1 - \bar
p)}.
}
\details{
The interim data of the exact framework (partial follow-up of
staggered enrollees) are mapped to an effective number of
completely observed subjects per arm,
\eqn{m = n_1 + n_2 (t - \bar t) / F}, the denominator of the
moment estimator in equation (4); the interim event-rate
estimates are then \code{x1 / m} per arm. This is the mapping used by
\code{\link[=thomas1991_example]{thomas1991_example()}}.
}
\examples{
halperin_conditional_power(371, 48.8, 2 / 48.8, 5 / 48.8)

}
\references{
Halperin M, Lan KKG, Ware JH, Johnson NJ, DeMets DL
(1982). An aid to data monitoring in long-term clinical
trials. Controlled Clinical Trials 3, 311-323.
}
