% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitored_oc.R
\name{monitored_operating_characteristics}
\alias{monitored_operating_characteristics}
\title{Operating characteristics of a conditional-power futility rule}
\usage{
monitored_operating_characteristics(
  n,
  m,
  tau,
  p_treatment,
  p_control,
  alpha = 0.05,
  driver = c("exact", "asymptotic"),
  region = NULL,
  cp_grid = NULL
)
}
\arguments{
\item{n}{Integer. Subjects per arm at the final analysis.}

\item{m}{Integer. Completely observed subjects per arm at the
interim analysis, \verb{0 < m < n}.}

\item{tau}{Numeric. Futility threshold: stop when conditional
power is below \code{tau}.}

\item{p_treatment}{Numeric. True treatment-arm event
probability used for the enumeration.}

\item{p_control}{Numeric. True control-arm event probability.}

\item{alpha}{Numeric. One-sided significance level.}

\item{driver}{Character. \code{'exact'} or \code{'asymptotic'}.}

\item{region}{Optional precomputed rejection region.}

\item{cp_grid}{Optional precomputed matrix from
\code{\link[=conditional_power_grid]{conditional_power_grid()}}; supplying it avoids recomputation
across thresholds and true-rate settings.}
}
\value{
A list: \code{reject} (probability of final rejection with
the futility rule in force), \code{stop} (probability of stopping
at the interim), and \code{ess} (expected total sample size over
both arms).
}
\description{
Computes, by exact enumeration over all interim states, the
operating characteristics of a monitored procedure that stops
for futility when current-trend conditional power falls below
\code{tau} at a single interim look with \code{m} completely observed
subjects per arm, and otherwise proceeds to the final one-sided
Fisher exact test on \code{n} subjects per arm. In the sense of
Lachin (2005) and Kunz and Kieser (2012), the futility rule can
only deflate the attained size and inflate the type II error;
this function quantifies both without approximation.
}
\references{
Lachin JM (2005). A review of methods for futility
stopping based on conditional power. Statistics in Medicine
24, 2747-2764. Kunz CU, Kieser M (2012). Curtailment in
single-arm two-stage phase II oncology trials. Biometrical
Journal 54, 445-456.
}
