% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/worked_example.R
\name{thomas1991_example}
\alias{thomas1991_example}
\title{Worked example of Section 4 of the manuscript}
\usage{
thomas1991_example(look = 1)
}
\arguments{
\item{look}{Integer, 1 or 2. Which interim look to evaluate.}
}
\value{
A list with components \code{estimates} (the
\code{\link[=estimate_partitioned_rates]{estimate_partitioned_rates()}} output for both arms),
\code{m_effective} (effective completely observed subjects per
arm), \code{cp_homogeneous} (equation 2), \code{cp_stratified}
(equation 9), and \code{cp_halperin} (the asymptotic comparator of
\code{\link[=halperin_conditional_power]{halperin_conditional_power()}} under the current trend).
}
\description{
Reproduces the manuscript's worked example: a hypothetical
two-arm mortality trial with uniform enrollment over
\code{entry_period = 3} years, follow-up horizon \code{follow_up = 5}
years, a one-sided Fisher exact test at \code{alpha = 0.05}, and the
Haseman exact sample size of 371 subjects per arm. Conditional
power is evaluated at each interim look by two routes: the
homogeneous plug-in of equation (2) with the moment estimators
of equations (4) to (6), and the exact stratified
Poisson-binomial calculation of equation (9) under an
exponential survival model with the same estimated marginal
rates and a uniform enrollment grid.
}
\details{
Conventions: the rejection region at the first look uses the
planned final sample size of 371 per arm; at the second look it
uses the achieved enrollment of 350 per arm.
}
\examples{
\donttest{
thomas1991_example(look = 1)$cp_stratified
}

}
