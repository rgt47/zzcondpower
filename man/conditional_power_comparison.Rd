% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison_study.R
\name{conditional_power_comparison}
\alias{conditional_power_comparison}
\title{Numerical comparison of exact and asymptotic conditional power}
\usage{
conditional_power_comparison(
  control_rate,
  reduction = 0.5,
  info_fractions = c(0.25, 0.5, 0.75),
  alpha = 0.05,
  power = 0.8,
  n_max = 5000
)
}
\arguments{
\item{control_rate}{Numeric vector of control-arm event
probabilities.}

\item{reduction}{Numeric. Relative risk reduction under the
alternative; the treatment rate is
\code{control_rate * (1 - reduction)}.}

\item{info_fractions}{Numeric vector of information fractions
at which to evaluate conditional power.}

\item{alpha}{Numeric. One-sided significance level.}

\item{power}{Numeric. Target design power.}

\item{n_max}{Integer. Upper bound for the sample-size search.}
}
\value{
A data frame with one row per scenario and information
fraction: \code{control_rate}, \code{treatment_rate}, \code{n},
\code{attained_size}, \code{fraction}, \code{x1}, \code{y1}, \code{cp_exact},
\code{cp_asymptotic}, and \code{difference}
(\code{cp_asymptotic - cp_exact}).
}
\description{
For a design scenario defined by a control-arm event rate and a
relative risk reduction, sizes the trial exactly for the target
power, then evaluates exact and asymptotic conditional power at
a set of information fractions with interim data equal to their
expectation under the alternative. The interim configuration
uses the clean mapping of completely observed subjects: at
information fraction \code{f}, \code{round(f * n)} subjects per arm are
fully observed and the remainder are unenrolled, so the exact
calculation is the Poisson-binomial of equation (9) with
two-valued weights and the asymptotic calculation is
\code{\link[=halperin_conditional_power]{halperin_conditional_power()}} with \code{m = round(f * n)}.
}
\examples{
\donttest{
conditional_power_comparison(0.20, info_fractions = 0.5)
}

}
