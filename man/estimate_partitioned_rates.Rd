% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{estimate_partitioned_rates}
\alias{estimate_partitioned_rates}
\title{Moment-based event-rate estimation under staggered entry
(equations 3 to 6)}
\usage{
estimate_partitioned_rates(
  x1,
  n_completed,
  n_on_study,
  t,
  entry_period,
  follow_up
)
}
\arguments{
\item{x1}{Integer. Events observed by the interim time \code{t}.}

\item{n_completed}{Integer. Subjects who have completed the full
follow-up period at \code{t} (\eqn{n_{j1}}).}

\item{n_on_study}{Integer. Subjects enrolled and still under
follow-up at \code{t} (\eqn{n_{j2}}).}

\item{t}{Numeric. Interim monitoring time (years from start).}

\item{entry_period}{Numeric. Planned enrollment period \code{E}.}

\item{follow_up}{Numeric. Follow-up horizon \code{F} per subject.}
}
\value{
A list with components \code{lambda_hat} (marginal event
rate, equation 4), \code{p1_hat} (pre-interim component, equation
5), \code{p2_hat} (post-interim component, equation 6), and \code{t_bar}
(mean enrollment time of on-study subjects).
}
\description{
Implements the manuscript's method-of-moments estimators for the
marginal event rate and its partition at an interim monitoring
time under staggered uniform enrollment and a constant hazard.
All arguments are vectorized over arms.
}
\examples{
estimate_partitioned_rates(x1 = c(2, 5), n_completed = 0,
                           n_on_study = 244, t = 2,
                           entry_period = 3, follow_up = 5)

}
