% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitored_oc.R
\name{conditional_power_grid}
\alias{conditional_power_grid}
\title{Current-trend conditional power over all interim states}
\usage{
conditional_power_grid(
  n,
  m,
  alpha = 0.05,
  driver = c("exact", "asymptotic"),
  region = NULL
)
}
\arguments{
\item{n}{Integer. Subjects per arm at the final analysis.}

\item{m}{Integer. Completely observed subjects per arm at the
interim analysis, \verb{0 < m < n}.}

\item{alpha}{Numeric. One-sided significance level.}

\item{driver}{Character. \code{'exact'} or \code{'asymptotic'}.}

\item{region}{Optional precomputed rejection region.}
}
\value{
An \verb{(m + 1) x (m + 1)} matrix; entry \verb{[x1 + 1, y1 + 1]}
is the current-trend conditional power at that interim state.
}
\description{
Computes, for every interim state \eqn{(x_1, y_1)} of a design
with \code{m} completely observed subjects per arm and \code{n - m} yet to
be enrolled, the current-trend conditional power of the final
one-sided Fisher exact test. The \code{exact} driver evaluates
equation (2) with trend rates \code{x1 / m} and \code{y1 / m}; the
\code{asymptotic} driver evaluates \code{\link[=halperin_conditional_power]{halperin_conditional_power()}} at
the same rates.
}
